import { StyleSheet } from "react-native";

export const AccountSettingStyles = StyleSheet.create({
  form: {
    padding: 10,
    marginTop: 20,
  },
});


export const AppSettingStyles = StyleSheet.create({
  form: {
    padding: 10,
    marginTop: 20,
    gap: 20,
  },
});

export const TeamSettingStyles = StyleSheet.create({
  form: {
    padding: 10,
    marginTop: 20,
  },
});
